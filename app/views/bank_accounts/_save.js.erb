$("ul.errors").html("");
	<% if @bank_account.errors.any? %>
	$('#div-error').addClass('alert alert-danger');
	 document.getElementById("div-error").innerHTML = "<h2><%= pluralize(@bank_account.errors.count, 'error') %> prohibited this zona from being saved:</h2>"+
	" <ul><% @bank_account.errors.full_messages.each do |message| %><li><%= message %></li><% end %></ul>";
<% else %>
	 $("#account_bancks_id").html("<%= escape_javascript(render(partial: 'bank_accounts/index', locals: { bank_accounts: @bank_accounts, show_actions: true })) %>");
	 $("#new-payment-modal").modal("hide");																											
<% end %>
	