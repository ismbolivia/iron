// var elemento = document.getElementById("div_hidden");
// elemento.className = "";
// var deposits = document.getElementById("div-deposits");
// deposits.className = "hidden";

$("ul.errors").html("");

<% if @deposit.errors.any? %>
	$('#div-error').addClass('alert alert-danger');
	document.getElementById("div-error").innerHTML = "<h2><%= pluralize(@deposit.errors.count, 'error') %> prohibited this zona from being saved:</h2>"+
	" <ul><% @deposit.errors.full_messages.each do |message| %><li><%= message %></li><% end %></ul>";

<% else %>
	$("#deposit_panel_id").html("<%= escape_javascript(render(partial: 'bank_accounts/table', locals: { bank_account: @bank_account, show_actions: true })) %>");
	$("#new-payment-modal").modal("hide");	
<% end %>
