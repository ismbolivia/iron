
var image = document.getElementById('item-image-<%= @item.id %>');
if (image) {
   image.src = "<%= @item.image.midium.url %>?" + new Date().getTime();
}
$(".modal").modal("hide");
if (typeof toastr !== 'undefined') {
  toastr.success("Imagen actualizada correctamente");
} else {
  alert("Imagen actualizada correctamente");
}