<%= form_with(model: purchase_order_lines_tax, local: true) do |form| %>
  <% if purchase_order_lines_tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase_order_lines_tax.errors.count, "error") %> prohibited this purchase_order_lines_tax from being saved:</h2>

      <ul>
      <% purchase_order_lines_tax.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :purchse_order_line_id %>
    <%= form.number_field :purchase_order_line_id %>
  </div>

  <div class="field">
    <%= form.label :tax_id %>
    <%= form.text_field :tax_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
