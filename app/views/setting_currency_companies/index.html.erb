<p id="notice"><%= notice %></p>

<h1>Setting Currency Companies</h1>

<table>
  <thead>
    <tr>
      <th>Company</th>
      <th>Currency</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @setting_currency_companies.each do |setting_currency_company| %>
      <tr>
        <td><%= setting_currency_company.company_id %></td>
        <td><%= setting_currency_company.currency_id %></td>
        <td><%= link_to 'Show', setting_currency_company %></td>
        <td><%= link_to 'Edit', edit_setting_currency_company_path(setting_currency_company) %></td>
        <td>
          <%= link_to 'Destroy', setting_currency_company, method: :delete, data: { confirm: '¿Estas seguro de eliminar?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Setting Currency Company', new_setting_currency_company_path %>
